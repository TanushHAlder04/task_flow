# Local development uses the Vite proxy (/api -> http://localhost:5000)
# In production on Netlify, set this to your deployed backend URL (e.g. https://your-backend.onrender.com/api)
VITE_API_BASE_URL=/api
